{
  "name": "gatezh-blog",
  "version": "1.0.0",
  "description": "Personal blog with Hugo and Cloudflare Workers",
  "private": true,
  "scripts": {
    "dev": "hugo server --appendPort=false --baseURL / --bind 0.0.0.0 -D",
    "build": "hugo"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "4.1.17",
    "tailwindcss": "4.1.17"
  }
}
