# Project name — must be unique across projects and match the sandbox variant
name: blog
services:
  devcontainer:
    image: ghcr.io/gatezh/devcontainers/claude-code:latest
    pull_policy: always
    volumes:
      - ..:/workspace:cached
