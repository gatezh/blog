{
  "name": "www",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "test -n \"$CODESPACE_NAME\" && hugo server --buildDrafts --appendPort=false --baseURL / --bind 0.0.0.0 || hugo server --buildDrafts --bind 0.0.0.0",
    "preview": "hugo server --appendPort=false --baseURL / --bind 0.0.0.0",
    "build": "hugo --minify",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@tailwindcss/cli": "4.1.18",
    "prettier": "3.5.3",
    "prettier-plugin-go-template": "0.0.15",
    "prettier-plugin-tailwindcss": "0.6.11",
    "tailwindcss": "4.1.18",
    "wrangler": "4.14.1"
  }
}
