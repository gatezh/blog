{
  "name": "@gatezh/api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "bun --env-file=../../.env.local x wrangler dev --port ${API_PORT:-8787}",
    "build": "wrangler deploy --dry-run --outdir=dist",
    "deploy": "wrangler deploy",
    "types": "wrangler types",
    "test": "bun test",
    "typecheck": "wrangler types && tsc --noEmit"
  },
  "dependencies": {
    "hono": "4.12.31",
    "resend": "6.18.0"
  },
  "devDependencies": {
    "@types/bun": "1.4.1",
    "@types/node": "catalog:",
    "wrangler": "catalog:"
  }
}
