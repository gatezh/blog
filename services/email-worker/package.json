{
  "name": "email-worker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "bun --env-file=../../.env.local x wrangler dev --port ${WORKER_PORT:-8787}",
    "build": "wrangler deploy --dry-run --outdir=dist",
    "deploy": "wrangler deploy",
    "types": "wrangler types",
    "typecheck": "tsc --noEmit",
    "lint": "oxlint ."
  },
  "dependencies": {
    "hono": "4.11.6",
    "resend": "4.5.1"
  },
  "devDependencies": {}

}
