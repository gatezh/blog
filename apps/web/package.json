{
  "name": "@gatezh/web",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "hugo server --buildDrafts --appendPort=false --baseURL / --bind 0.0.0.0",
    "build": "hugo --minify",
    "preview": "hugo server --appendPort=false --baseURL / --bind 0.0.0.0",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "prettier": "3.5.3",
    "prettier-plugin-go-template": "0.0.15",
    "prettier-plugin-tailwindcss": "0.6.11",
    "wrangler": "4.14.1"
  }
}
